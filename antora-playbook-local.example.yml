# Example / pointer only.
# Prefer the committed antora-playbook-local.yml in this repo (sibling checkouts, no extends).
# Stock Antora does not support playbook extends — that was an experimental fork feature.
#
# Run:
#   pnpm exec antora antora-playbook-local.yml
#
# Or for CI/production-shaped sources:
#   pnpm exec antora antora-playbook.yml
